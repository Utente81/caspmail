spec:
  template:
    spec:
      containers:
      - name: backend
        volumeMounts:
        - mountPath: /app/migrations
          name: backend-migrations
      volumes:
      - name: backend-migrations
        hostPath:
          path: /home/ubuntu/caspmail/new/backend/migrations
          type: Directory
