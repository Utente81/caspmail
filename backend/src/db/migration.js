const { Pool } = require('pg'); const pool = new Pool({ connectionString: process.env.DATABASE_URL }); async function run() { try { await pool.query('CREATE TABLE IF NOT EXISTS e2ee_folders (id UUID PRIMARY KEY, tenant_id UUID NOT NULL, user_email VARCHAR(255) NOT NULL, name VARCHAR(255) NOT NULL, created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW());'); console.log('Migration successful'); } catch(e) { console.error(e); } finally { await pool.end(); } } run();
