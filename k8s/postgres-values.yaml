global:
  postgresql:
    auth:
      postgresPassword: ***REDACTED***
      password: ***REDACTED***
      database: caspermail
      username: caspermail
architecture: replication
primary:
  persistence:
    size: 2Gi
readReplicas:
  replicaCount: 2
  persistence:
    size: 2Gi
tls:
  enabled: true
  autoGenerated: true
